---
import Layout from "@/layouts/Layout.astro";
import { getReadme } from "@/lib/github";
import Head from "@/components/head-page.astro";
const { html, etag } = await getReadme();

Astro.cache.set({
  maxAge: 120,
  swr: 60,
  tags: ["home"],
});
---

<Layout>
  <Head slot="head" title="Home" />
  <div set:html={html} />
</Layout>
